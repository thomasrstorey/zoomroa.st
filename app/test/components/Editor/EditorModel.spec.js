import EditorModel from '../../../src/components/Editor/EditorModel';

describe('EditorModel', () => {
  describe('.initialize', () => {
    it('sets an event listener for drop event');
  });

  describe('.onDrop', () => {
    it('ingests the given file and sets the image attribute on the model');
  });
});
